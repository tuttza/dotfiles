# Ruby - rbenv:
status --is-interactive; and source (rbenv init -|psub)

# Functions:


# GLOBAL ENV VARS:
set -g -x KUBE_EDITOR kak
set -g -x SHELL /usr/local/bin/fish

# Aliases:
alias rbc="ruby -c"
alias fs="grep -rni"
alias gs="git status"
alias ga="git add"
